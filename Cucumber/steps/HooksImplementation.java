package steps;

import java.time.Duration;

import org.openqa.selenium.chrome.ChromeDriver;

import io.cucumber.java.After;
import io.cucumber.java.Before;
import io.github.bonigarcia.wdm.WebDriverManager;

public class HooksImplementation extends BaseClass{
	
	
	
	//to execute before each scenario
	/*
	 * @Before public void preCondition() { WebDriverManager.chromedriver().setup();
	 * driver = new ChromeDriver(); // driver is initialized
	 * driver.manage().window().maximize();
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(5));
	 * System.out.println(driver); driver.get("http://leaftaps.com/opentaps");
	 * 
	 * }
	 * 
	 * //to execute after each scenario
	 * 
	 * @After public void postCondition() { driver.close(); }
	 */

}
